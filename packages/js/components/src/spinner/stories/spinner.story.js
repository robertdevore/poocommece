/**
 * External dependencies
 */
import { Spinner } from '@poocommerce/components';

export const Basic = () => (
	<div>
		<Spinner />
	</div>
);

export default {
	title: 'Components/Spinner',
	component: Spinner,
};
